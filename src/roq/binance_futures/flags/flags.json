{
  "name": "Flags",
  "type": "flags",
  "values": [
    {
      "name": "exchange",
      "type": "std::string",
      "required": true,
      "default": "binance-futures",
      "description": "Exchange identifier (string)"
    },
    {
      "name": "api",
      "type": "std::string",
      "required": true,
      "default": "fapi",
      "description": "What api to use (fapi, dapi)"
    }
  ]
}
