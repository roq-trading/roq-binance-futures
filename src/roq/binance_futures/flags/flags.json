{
  "name": "Flags",
  "type": "flags",
  "values": [
    {
      "name": "exchange",
      "type": "std::string",
      "required": true,
      "default": "binance-futures",
      "description": "exchange identifier (string)"
    },
    {
      "name": "api",
      "type": "std::string",
      "required": true,
      "default": "fapi",
      "description": "what api to use (fapi, dapi)"
    },
    {
      "name": "enable_portfolio_margin",
      "type": "bool",
      "default": false,
      "description": "enable portfolio margin mode? (papi)"
    }
  ]
}
