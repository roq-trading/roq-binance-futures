{
  "name": "Flags",
  "type": "flags",
  "values": [
    {
      "name": "exchange",
      "type": "std::string",
      "required": true,
      "default": "binance-futures",
      "description": "Exchange identifier"
    },
    {
      "name": "api",
      "type": "std::string",
      "required": true,
      "default": "fapi",
      "description": "API to use (:code:`fapi` or :code:`dapi`)"
    }
  ]
}
